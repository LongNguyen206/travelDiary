    <div class="container">
        <div class="row">
            <div class="col s12">
                <div class="card blue-grey darken-1">
                    <div class="card-content white-text">
                        <span class="card-title">{{trip.country}}</span>
                        <p>by {{trip.user.name}}</p>
                        <br>
                        <p>{{trip.description}}</p>
                    </div>
                    <div class="card-action">
                        <a href="#">User Profile</a>
                    </div>
                </div>
            </div>
        </div>
    </div>